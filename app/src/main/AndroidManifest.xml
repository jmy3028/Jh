<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="study01.practice.com.jhstudy01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ScoreboardActivity" />
        <activity android:name=".activity.RockPaperScissorsActivity" />
        <activity android:name=".activity.login01.Login01Activity" />
        <activity android:name=".activity.login01.LoginResultActivity" />
        <activity android:name=".activity.login02.Login02Activity" />
        <activity android:name=".activity.login02.Login02ResultActivity"></activity>
    </application>

</manifest>